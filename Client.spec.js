const expect = require('chai').expect;
import mapper from './client/mapper';
import App from './client/App';
describe('Client', ()=> {
  it('can use import', ()=> {
    expect(true).to.equal(true);
  });
});
